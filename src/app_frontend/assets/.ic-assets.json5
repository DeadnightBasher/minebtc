[
    {
        "match": "**/*",

        // Disables the security policy to allow full flexibility for testing.
        // Be cautious in production; this setting allows all scripts, styles, and frames.
        "security_policy": "disabled",

        // Suppresses warnings about security policies.
        "disable_security_policy_warning": true,

        // Uncomment to redirect all requests from .raw.icp0.io to .icp0.io
        // "allow_raw_access": false
    }
]
